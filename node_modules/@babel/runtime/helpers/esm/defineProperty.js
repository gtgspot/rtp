export async function getStaticProps() {
  const data = await fetch('https://your-api.com/rtp-data').then(res => res.json());

  return {
    props: { data },
    revalidate: 60, // Regenerate the page every 60 seconds
  };
}
